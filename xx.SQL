CREATE TABLE `AHAHAH` (
    `id` int(11) NOT NULL AUTO_INCREMENT 'CCC',
    `emodd` varchar(11) DEFAULT NULL COMMENT 'SSS',
    PRIMARY KEY (`ID`)
);
CREATE TABLE `AHAHAH` (
    `id` int(11) NOT NULL AUTO_INCREMENT 'CCC',
    `emodd` varchar(11) DEFAULT NULL COMMENT 'SSS',
    PRIMARY KEY (`ID`)
);

CREATE TABLE `AHAHAH` (
    `id` int(11) NOT NULL AUTO_INCREMENT 'CCC',
    `emodd` varchar(11) DEFAULT NULL COMMENT 'SSS',
    PRIMARY KEY (`ID`)
);
CREATE TABLE `AHAHAH` (
    `id` int(11) NOT NULL AUTO_INCREMENT 'CCC',
    `emodd` varchar(11) DEFAULT NULL COMMENT 'SSS',
    PRIMARY KEY (`ID`)
);